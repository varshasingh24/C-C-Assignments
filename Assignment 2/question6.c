// 6. Write a program which takes a character as an input and displays its ASCII code.
#include<stdio.h>

int main(){
    char a;
    printf("Enter the character \n");
    scanf("%c",&a);
    printf("ASCII code of the given character is %d", a);
    return 0;
}